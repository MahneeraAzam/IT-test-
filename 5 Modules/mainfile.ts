// ## Task 3:
// - create main.ts file. import data from both file and add both values.
// print it into terminal.

import * as index from './index.js'

import * as index2 from './index2.js'

const a = index.firstNumber;

const b = index2.secondNumber;

let total = a + b  

console.log(total); 

